物品nbt支持解析（药箭药水附魔飞行时间下落的方块类型等）
支持通过json配置文件内的配置对投影方块等进行可选转换
支持在tile entity container内有潜影盒的情况下，或者潜影盒作为t.e.c.（前面的简写）的情况下，读取内部物品的时候支持标注容器内的潜影盒内的物品所属为潜影盒，并显示嵌套容器（如果有），同时每种物品标注是哪种容器内的，并在最后有个合并列表输出总物品数目。
添加程序默认行为配置文件（json格式），支持一些映射表转换开关，支持部分输出格式开关等（如关闭容器输出，方块带状态输出，只输出多区域总合等）
添加nbt write方法，添加与mc源码类似的nbt操作方法如putcompound等，以方便程序创建nbt对象（而不是使用及其不优雅的逆天强类型构造），添加方块替换支持，添加1.21部分容器nbt解析，添加收纳袋1.21解析，添加从1.21版本投影强制降低版本功能（保留所有能保留或可能存在低版本移植容器！）
如果有堆肥、讲台翻页、点火等情况下，会通过转换后额外区域告知用户以便提前准备
容器内物品替换功能：把A类型的容器内符合B条件的物品替换成C，或把A类型的容器替换成B类型的容器（包括方块实体），容器大小不匹配的情况下损失部分超出数据（可预期）
方块替换功能：把A类型的方块替换成B类型的方块（保留状态或默认状态或指定状态）
实体替换功能：还没想好
方块实体替换功能：还没想好
实体转换物品还是有必要的，比如船，是什么材质由实体nbt决定，而实体名都叫boat


（此项可能无限延期）转换通过json配置文件映射不同版本
转换为txt格式

添加物品堆叠逻辑注册表，针对不同堆叠属性物品进行箱盒显示转换（多版本支持+json配置提上日程）
